<h1><%= @category.name %></h1>
<div class="row">
  <% @recipes_by_cat.each do |recipe| %>

    <div class="col-sm-6 col-md-4">
      <div class="well text-center">
        <% if recipe.image.nil? == false %>
          <p><%= image_tag recipe.image, size: 250 %></p>
        <% end %>
        <h3><%= link_to recipe.name, recipe %></h3>
        <p>
          <a href="#" data-toggle="modal" data-target="#myModal_<%= recipe.id %>">
            <span class="glyphicon "></span>Sneak Peek</a>
        </p>
      </div>
    </div>

    <!-- Recipe Modal -->

    <div class="recipe-modal">

      <div class="modal fade" id="myModal_<%= recipe.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span></button>
              <h3 class="modal-title" id="myModalLabel"><%= recipe.name %> <div style="text-align: center"><iframe src="https://open.spotify.com/embed?uri=spotify:album:1DFixLWuPkv3KT3TnV35m3" width="400" height="75" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe></div></h3>
            </div>
            <div class="modal-body">
              <div class="modal-image">
                <% if recipe.image.nil? == false %>
                  <p><%= image_tag recipe.image, size: 150 %></p>
                <% end %>
              </div>
              <p>
                <strong>Description:</strong>
                <%= recipe.description%></p>
              <p>
                <strong>Ingredients:</strong>
                <%= recipe.ingredients %></p>
              <p>
                <strong>Directions:</strong>
                <%= recipe.directions %></p>

            </div>

          </div>
        </div>
      </div>
    </div>



  <% end %>
</div>

</div>
